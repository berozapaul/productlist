import React from 'react';

// Application context that is used for dialog, data sharing
// among components, as global variables
const AppContext = React.createContext();

export default AppContext;
